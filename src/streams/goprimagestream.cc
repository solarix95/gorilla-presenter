#include "streams/goprimagestream.h"

//-------------------------------------------------------------------------------------------------
GoPrImageStream::GoPrImageStream()
{
}

//-------------------------------------------------------------------------------------------------
void GoPrImageStream::init()
{
}

//-------------------------------------------------------------------------------------------------
void GoPrImageStream::stop()
{
}

//-------------------------------------------------------------------------------------------------
QString GoPrImageStream::source(const QString &url)
{
    if (!url.startsWith("stream://"))
        return "";
    return url.mid(9);
}

